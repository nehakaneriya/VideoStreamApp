# ============================================================
# VideoStreamApp — Docker Compose Environment Variables
# ============================================================
# Copy karo: cp .env.example .env
# Phir values fill karo, .env ko kabhi commit mat karna
# ============================================================

# ── DATABASE ─────────────────────────────────────────────────
DB_NAME=videostreamdb
DB_PASSWORD=root

# ── JWT ──────────────────────────────────────────────────────
# 64+ characters ka secret — apna khud generate karo
JWT_SECRET=changeme_generate_a_64_char_random_secret_key_for_production_use
JWT_ISSUER=videostream.com
JWT_ACCESS_TTL_SECONDS=300
JWT_REFRESH_TTL_SECONDS=86400
JWT_REFRESH_COOKIE_NAME=refreshToken
JWT_ADMIN_REFRESH_COOKIE_NAME=adminRefreshToken
JWT_COOKIE_SECURE=false
JWT_COOKIE_HTTP_ONLY=true
JWT_COOKIE_SAME_SITE=lax
JWT_COOKIE_DOMAIN=localhost

# ── ADMIN CREDENTIALS ────────────────────────────────────────
ADMIN_EMAIL=admin@gmail.com
ADMIN_PASSWORD=admin123

# ── FRONTEND URLS ─────────────────────────────────────────────
FRONT_END_URL=http://localhost:1304
FRONT_END_SUCCESS_REDIRECT=http://localhost:1304/oauth/success
FRONT_END_FAILURE_REDIRECT=http://localhost:1304/oauth/failure

# ── GOOGLE OAUTH2 ─────────────────────────────────────────────
GOOGLE_CLIENT_ID=test-client-id
GOOGLE_CLIENT_SECRET=test-client-secret

# ── GITHUB OAUTH2 ─────────────────────────────────────────────
GITHUB_CLIENT_ID=test-client-id
GITHUB_CLIENT_SECRET=test-client-secret

# ── SPRING PROFILE ───────────────────────────────────────────
SPRING_PROFILES_ACTIVE=dev
